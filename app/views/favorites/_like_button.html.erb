<% if logged_in? %>
  <% if current_user.like?(post) %>
    <%= form_with(model: current_user.favorites.find_by(post_id: post.id), local: true, method: :delete) do |f| %>
      <%= hidden_field_tag :user_id, current_user.id, id: "user_id_#{rand(1..100000)}" %>
      <%= hidden_field_tag :post_id, post.id, id: "post_id_#{rand(1..100000)}" %>
      <%= f.submit 'Unlike', class: 'btn btn-danger btn-block' %>
    <% end %>
  <% else %>
    <%= form_with(model: current_user.favorites.build, local: true) do |f| %>
      <%= hidden_field_tag :user_id, current_user.id, id: "user_id__#{rand(1..100000)}" %>
      <%= hidden_field_tag :post_id, post.id, id: "post_id_#{rand(1..100000)}" %>
      <%= f.submit 'Like', class: 'btn btn-primary btn-block' %>
    <% end %>
  <% end %>
<% else %>
<% end %>